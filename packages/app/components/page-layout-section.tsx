import { styled } from 'tamagui'
import { XStack } from '@my/ui'

export const PageLayoutSection = styled(XStack, {
  alignItems: 'center',
  justifyContent: 'space-between',
  flexWrap: 'wrap',
  width: '100%',
})
