import { styled } from 'tamagui'
import { XStack } from '@my/ui'

export const PageLayoutFooter = styled(XStack, {
  alignItems: 'center',
  justifyContent: 'center',
  backgroundColor: '#D31E66',
  height: 200,
  width: '100%',
})
